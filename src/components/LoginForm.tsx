"use client";
import { useState } from "react";

export default function LoginForm(){
  const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);
  async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||d?.error||'No se pudo iniciar sesión');location.href='/';}catch(e){setError(e instanceof Error?e.message:'Error');setBusy(false)}}
  return <main className="login-shell"><section className="login-card"><div className="live-brand"><div className="live-logo">J</div><div><strong>JFMCSS</strong><span>CONTROL</span></div></div><div className="eyebrow">OPERATIONS OS</div><h1>Acceso seguro</h1><p>CRM, proyectos, facturación, cobros, soporte y operaciones en un solo lugar.</p><form onSubmit={submit}><label>Correo<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><label>Contraseña<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn login-submit" disabled={busy}>{busy?'Validando…':'Entrar a JFMCSS Control'}</button></form><small>El primer acceso puede crear el Super Admin usando BOOTSTRAP_ADMIN_EMAIL y BOOTSTRAP_ADMIN_PASSWORD.</small></section></main>
}
